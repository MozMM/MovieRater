const Sequelize = require('sequelize')
const db = require('../db')

const Ratings = require('./ratings')

// ** if scaled up with further models, define associations here. ** //

module.exports = {
  Ratings
}